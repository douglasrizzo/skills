repos:
  - repo: local
    hooks:
      - id: build-dist
        name: Rebuild dist outputs
        entry: ./scripts/build.sh
        language: system
        pass_filenames: false
        files: ^(fragments/.*|scripts/build\.sh)$
